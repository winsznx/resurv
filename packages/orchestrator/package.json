{
  "name": "@resurv/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The measured attempt lifecycle, executed. Persists before it sends, reconciles against chain, and never advances a covenant on an HTTP status.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@resurv/chain": "workspace:*",
    "@resurv/domain": "workspace:*",
    "@resurv/keeperhub-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
