{
  "name": "@resurv/node-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Host-process concerns for Node entry points: locating the repository root and loading the one local credential. Never imported by the Worker or the browser bundle.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
