{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // WebCrypto and TextEncoder are globals in both Node 24 and workerd. @types/node
    // declares them without pulling in the DOM lib, which this package must not depend on.
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
